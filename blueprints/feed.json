{
  "id": "feed",
  "purpose": "Browse a stream of items and pick one.",
  "layout": "Optional search or category chips pinned near the top, then a vertical stream of rich cards (image, title, 1–2 meta lines). A featured row may scroll horizontally above the main stream.",
  "sections": {
    "required": [
      "Category chips or filter row",
      "Vertical stream of item cards with image, title and meta"
    ],
    "optional": [
      "Search field",
      "Featured horizontal carousel",
      "Section headers with \"See all\"",
      "Promo banner (one, not repeated)"
    ]
  },
  "primaryAction": {
    "placement": "none",
    "note": "Tapping a card is the action; no filled button on the screen."
  },
  "avoid": [
    "Every card the same height with no featured item",
    "Text-only cards when the items are visual",
    "Stacking three carousels"
  ],
  "hig": [
    "search",
    "chip",
    "card",
    "image",
    "list"
  ],
  "variants": [
    {
      "id": "a",
      "layout": "Stream of large cards: full-width image cards stacked vertically, each with title and 1–2 meta lines."
    },
    {
      "id": "b",
      "layout": "Grid: a two-column grid of compact tiles (square image, title, one meta line), with one wide featured tile at the top."
    },
    {
      "id": "c",
      "layout": "Editorial: one big hero item with an overlaid title, then the rest as horizontal rows (small thumbnail left, text right)."
    }
  ]
}
